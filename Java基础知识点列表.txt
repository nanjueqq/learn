----------------Java-------------
Java基本数据类型（名字，包装类，长度）
Java访问控制符（名字，范围）
Java程序流程控制：if／for／while（继续和跳出，each）
继承与接口（定义，区别，使用）
异常（常见异常，捕获方式，方法级抛出，自定义异常）
基本字符串处理（切分，查找）
集合类：基本使用方法

面向对象特性：
	封装——目标，高内聚、低耦合。将细节封装在类中，对外只暴露一些方法。比如将属性设置为private，用get/set方法来存取。
	继承——目标，使资源（属性、方法）可以得到复用，减少代码量（编写量和修改量）。
	多态——目标，也是降低代码量。实现方式是让代码在运行时表现出不同的特性。

重写和重载
==和equals
注释的几种写法
git使用方式

-------------Java end-------------







-------------Spring---------------
-------------Spring end-----------